name: LambdaDeploy
on:
  push:
    branches:
      - *
jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [14.x]

    steps:
      # 環境構築
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}

      # パッケージのキャッシュ
      - name: Use cached node_modules
        uses: actions/cache@v2
        with:
          path: |
            demo/server/node_modules
          key: ${{ runner.os }}-server-v6-dependencies-${{ hashFiles('demo/server/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-server-v6-dependencies-

      # パッケージインストール
      - run: |
          cd demo/server
          npm ci
          npm run build

      # serverless frameworkのインストール
      - name: Install Serverless Framework
        run: npm install -g serverless
      - name: Serverless Authentication
        run: sls config credentials -k ${{ secrets.AWS_ACCESS_KEY }} -s ${{ secrets.AWS_SECRET_KEY }} -p aws

      # APIのデプロイ
      - name: Deploy to AWS
        run: sls deploy
        working-directory: "./demo/server"
