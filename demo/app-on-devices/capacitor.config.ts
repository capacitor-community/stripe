import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.capacitor.stripe.devices',
  appName: 'app-on-devices',
  webDir: 'www',
};

export default config;
