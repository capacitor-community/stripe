import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.capacitor.stripe',
  appName: 'capacitor-stripe',
  webDir: 'www/browser',
};

export default config;
