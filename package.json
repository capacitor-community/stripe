{
  "name": "@capacitor-community/stripe",
  "version": "5.0.3",
  "engines": {
    "node": ">=16.0.0"
  },
  "type": "module",
  "private": false,
  "description": "Stripe SDK bindings for Capacitor Applications",
  "scripts": {
    "release": "np --release-draft-only && node ./release.js"
  },
  "devDependencies": {
    "np": "^7.5.0",
    "write-pkg": "^6.0.0"
  }
}
